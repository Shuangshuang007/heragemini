# 🚀 Career Switch API 部署检查清单

## ✅ 部署前准备

- [ ] 确认Vultr服务器IP地址: `___________________`
- [ ] 确认MongoDB连接字符串: `___________________`
- [ ] 确认OpenAI API Key: `sk-proj-___________________`
- [ ] 确认Resume PDF服务端口 (避免冲突): `___________________`
- [ ] 确认Cover Letter服务端口 (避免冲突): `___________________`

---

## 📦 步骤1: 打包上传

```bash
# 本地执行
cd /Users/shuangshuangwu/Desktop
tar -czf career_switch_deploy.tar.gz career_switch_deploy/
scp career_switch_deploy.tar.gz root@YOUR_VULTR_IP:/opt/
```

- [ ] 打包成功
- [ ] 上传成功

---

## 🔧 步骤2: 服务器配置

```bash
# SSH登录
ssh root@YOUR_VULTR_IP

# 解压
cd /opt
tar -xzf career_switch_deploy.tar.gz
cd career_switch_deploy

# 配置环境变量
cp .env.example .env
nano .env  # 填写实际值

# 安装依赖
npm install
```

- [ ] SSH登录成功
- [ ] 解压成功
- [ ] .env配置完成
- [ ] npm install成功

---

## 🚀 步骤3: 启动服务

```bash
# 启动PM2
pm2 start ecosystem.config.js

# 检查状态
pm2 status

# 查看日志
pm2 logs career-switch-api --lines 50
```

- [ ] PM2启动成功
- [ ] 进程状态: `online`
- [ ] 日志无错误

---

## 🔒 步骤4: 防火墙配置

```bash
# 开放端口3009
ufw allow 3009/tcp
# 或在Vultr控制台配置
```

- [ ] 端口3009已开放

---

## 🧪 步骤5: 测试API

```bash
# 本地测试
curl http://localhost:3009/health

# 外网测试
curl http://YOUR_VULTR_IP:3009/health
```

预期输出:
```json
{
  "status": "healthy",
  "service": "Héra Career Switch API",
  "graphStats": {
    "nodes": 51283,
    "edges": 134755
  }
}
```

- [ ] 本地访问成功
- [ ] 外网访问成功
- [ ] 返回正确的统计数据

---

## 📊 步骤6: 功能测试

```bash
# 测试raw模式
curl -X POST http://YOUR_VULTR_IP:3009/api/career/advice \
  -H "Content-Type: application/json" \
  -d '{
    "currentJob": "Software Engineer",
    "experience": 3,
    "mode": "raw"
  }' | jq

# 测试report模式 (需要OpenAI Key)
curl -X POST http://YOUR_VULTR_IP:3009/api/career/advice \
  -H "Content-Type: application/json" \
  -d '{
    "currentJob": "Product Manager",
    "experience": 2,
    "mode": "report"
  }' | jq
```

- [ ] raw模式返回候选职位列表
- [ ] report模式返回完整GPT报告
- [ ] 报告包含 "Héra" (带重音符号)
- [ ] signature为 "Report generated by Héra AI"

---

## 🔗 步骤7: 集成Heraai

记录API地址:
```
CAREER_SWITCH_API_URL=http://YOUR_VULTR_IP:3009
```

- [ ] 已记录API地址
- [ ] 准备配置到Heraai .env.local
- [ ] 准备配置到Vercel环境变量

---

## ✅ 部署完成确认

- [ ] API在Vultr正常运行
- [ ] PM2设置开机自启: `pm2 save && pm2 startup`
- [ ] 防火墙规则已保存
- [ ] API地址已记录
- [ ] 所有测试通过

---

## 🎯 下一步: Heraai集成

1. 修改 `/api/mcp/route.ts` 添加MCP工具
2. 创建 `/api/assistant/career-advice/route.ts`
3. 配置环境变量 `CAREER_SWITCH_API_URL`
4. 本地测试
5. 部署到Vercel

---

## 📝 部署信息 (填写记录)

```
部署日期: ____________________
服务器IP: ____________________
API端口: 3009
PM2进程名: career-switch-api
日志路径: /opt/career_switch_deploy/logs/
数据路径: /opt/career_switch_deploy/graph_data/

测试链接:
- Health: http://____________________:3009/health
- Stats: http://____________________:3009/api/career/stats
- Advice: http://____________________:3009/api/career/advice

部署人: ____________________
备注: ____________________
```

